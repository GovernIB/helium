package es.caib.helium.dada.repository;

public class DadaSpecifications {

//    public static Specification<Dada> isNomLike(String nom) {
//        return (base, cq, cb) -> cb.like(cb.lower(base.get("nom")), "%" + nom.toLowerCase() + "%");
//    }
//
//    public static Specification<Dada> isNomLikeAndCodiEquals(String nom, String codi) {
//        return (base, cq, cb) -> cb.and(
//                isNomLike(nom).toPredicate(base, cq, cb),
//                cb.equal(base.get("codi"), codi)
//        );
//    }
    
}
